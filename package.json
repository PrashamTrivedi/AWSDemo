{
  "name": "awsjs",
  "version": "1.0.0",
  "description": "",
  "main": "src/first.js",
  "scripts": {
    "zip": "rm function.zip & zip -r function.zip src/* node_modules/*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-xray-sdk": "^3.5.0"
  }
}
